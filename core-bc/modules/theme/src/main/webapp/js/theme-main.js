AUI().ready('aui-base', 'vgrintra-theme-main', function(A) {
  var vgrIntraThemeMain = new A.VgrIntraThemeMain().render();
});

/*
AUI().ready('aui-base', 'ifeed-parser', function(A) {

  var ifeedCtn = A.all('.ifeed-parsed-ctn');

  ifeedCtn.each(function(item, index, list) {

    var ifeedParser = new A.IfeedParser({
      container: item
    }).render();

  });


});
*/
